apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ts-example-pro
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: ts-example-site-app
    files:
      - server.js=server.js
